# === Database ===
DATABASE_URL=postgresql+asyncpg://mediapp_user:mediapp_pass@db:5432/mediapp_db

# === JWT ===
JWT_SECRET=change-me-to-a-random-256-bit-hex-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# === Encryption (AES-256-GCM) ===
ENCRYPTION_KEY=change-me-to-a-random-64-char-hex-string

# === App ===
ENVIRONMENT=development
FRONTEND_URL=http://localhost:5173
API_PREFIX=/api/v1

# === Email (Resend) ===
RESEND_API_KEY=re_dummy_key_for_dev
FROM_EMAIL=onboarding@resend.dev
